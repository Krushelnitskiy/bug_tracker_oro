oro_dashboard_config:
    widgets:
        issues_grid:                               # register a widget
            label:      oro.tracker.issue.dashboard.issues_grid.label
            route:      oro_dashboard_widget        # you can use existing controller to render your TWIG template
            route_parameters: { bundle: OroTrackerBundle, name: grid }   # just specify a bundle and a TWIG template name
#            acl:        orocrm_call_view
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('orotracker_issue_user_issue', { userId: entity.id }),
        'title': 'oro.tracker.issue.user_issue'|trans
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}
